# Secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt
secrets/
credentials/
*_token*
*_secret*
*_key*

# n8n runtime / exports
n8n/.n8n/
n8n/database.sqlite
n8n/*.sqlite
n8n/*.db

# Backups
backups/*
!backups/README.md

# Logs
*.log
logs/

# OS / editor
.DS_Store
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Docker
docker-compose.override.yml

# Claude Code local settings
.claude/settings.local.json
.claude/*.local.json

# Real deployment files
n8n.env
docker-compose.yml
/opt/n8n/
*.tar.gz

# Operator-local installation-specific runtime workflow id map (real n8n ids — never commit).
# Only the schema example (config/runtime_ids.example.json) and resolver code are committed.
config/runtime_ids.local.json
config/runtime_ids.local.json.bak.*
# Sanitized release evidence (fingerprints only; keep installation reports out of the repo).
release-evidence/
.release.lock
