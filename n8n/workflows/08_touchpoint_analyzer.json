{
  "name": "08 - Touchpoint Analyzer",
  "nodes": [
    {
      "id": "a8000000-0000-0000-0000-0000000000n1",
      "name": "Overview Note RU",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -540,
        -320
      ],
      "parameters": {
        "content": "## 08 — Touchpoint Analyzer (Stage 3.2, PATCHED v3 — DETERMINISTIC-FIRST) — Business Scout Agent\n\nSource-agnostic анализатор. Читает approved/unique из raw_market_records, классифицирует и маршрутизирует в 6 бизнес-вкладок (35 колонок).\n\nПАТЧ v3 (DEC-082): второй live-тест показал primary_json=0, дорогой и нестабильный LLM ($0.159 за 12 записей). Теперь анализатор ДЕТЕРМИНИРОВАННО-ПЕРВЫЙ. Claude — ОПЦИОНАЛЬНОЕ обогащение, ВЫКЛ по умолчанию.\n\nSet Analyzer Config: analysis_mode='deterministic_first', llm_enrichment=false, max_records=12, test_mode=true.\n\nЛогика:\n- Prepare Record считает ДЕТЕРМИНИРОВАННУЮ классификацию (det) для КАЖДОЙ записи по подсказкам приёма.\n- LLM-шлюз (IF Call Claude?): call_claude = (НЕ irrelevant) И (llm_enrichment=true ИЛИ deterministic_needs_llm=true).\n- При deterministic_first + llm_enrichment=false классифицируемые записи идут в Build Deterministic Row БЕЗ Claude ($0): irrelevant → deterministic_irrelevant_skip; остальные → deterministic_pre_route.\n- Claude вызывается только если det неуверен (deterministic_needs_llm=true) или включено обогащение. После Claude: primary → repair → детерминированный fallback (deterministic_fallback_after_llm_fail). technical_errors — только если det без валидного route или сбой Sheets/API.\n\nДля 12-записного фикстура (deterministic_first, enrichment off): Claude calls=0, repair_used=0, technical_errors=0.\n\nБудущий режим: analysis_mode='llm_enriched' + llm_enrichment=true → Claude обогащает не-irrelevant записи, fallback по-прежнему действует.\n\nparse_method: deterministic_pre_route | deterministic_irrelevant_skip | primary_json | repaired_json | deterministic_fallback_after_llm_fail | technical_error.\n\nПАТЧ v6 (DEC-087, качество обогащения после C2 PARTIAL PASS): (A) короткий компактный промпт для source_candidate/review_source (Telegram/Zoon) + детерминированные HINTS (expected_entity_type/action/route, no_contact_safety, forbidden_phrases) в payload. (B) review_source: только НАЗВАННЫЙ конкурент (competitor_name) → monitor_queue; обобщённый каталог/категория отзывов (например, Zoon «кредитные брокеры в Москве») → content_idea/content_queue, competitor_strength ≤45. (C) без публичного контакта reason НЕ содержит «обратиться напрямую/написать/позвонить/связаться» — санитайзер в Merge заменяет на «требует ручной проверки». (D) запрет необоснованных «спрос растёт/много лидов/высокая конверсия». max_tokens НЕ увеличен. Цель C3: primary_json≥3/4, fallback≤1/4, technical_errors=0.\n\nПАТЧ v7 (DEC-088, C4 — стабилизация JSON для Telegram/source_candidate): для source_candidate / social_channel / telegram-content_idea используется СПЕЦИАЛИЗИРОВАННАЯ УЛЬТРА-КОРОТКАЯ схема из 7 ключей (profile_name, service_type, offer_text, detected_need, reason, content_idea_score, quality_score) — отдельный системный промпт и минимальный payload в Build Primary Claude Request; меньшая поверхность ответа повышает надёжность строгого JSON и убирает fallback Telegram. Repair для этого семейства чинит в те же 7 ключей (не раздувает до 15). Merge накладывает только эти 7 полей, а route=content_queue / entity_type=content_idea / recommended_action=create_content / lead_signal_score=1 / competitor_strength=1 / contact_public='' остаются детерминированными. Пост-merge safety assertion (Task D) гарантирует это для social-источников. max_tokens специализированного пути 500/400 (НЕ увеличивает стоимость). Цель C4: primary_json≥3/4, Telegram не fallback, technical_errors=0.\n\nНЕ парсер, НЕ скрейпинг, НЕ Apify/Firecrawl. Только Claude HTTP (gated). active=false.",
        "height": 700,
        "width": 660,
        "color": 4
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000n2",
      "name": "Test Instructions RU",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        160,
        -320
      ],
      "parameters": {
        "content": "## Настройка и тест (вручную)\n\n1. НЕ активировать (active=false).\n2. Вкладки: 6 бизнес-вкладок (35 колонок) + raw_market_records (40). Уже существуют.\n3. ПОСЛЕ ИМПОРТА перепривязать креденшлы (ID локальны):\n   - Claude Primary / Claude Repair → Claude API - Marketing Scout\n   - Read raw_market_records / Append to Dynamic Route Sheet → Google Sheets - Marketing Scout Service Account\n4. Заменить PASTE_SPREADSHEET_ID_HERE на реальный Spreadsheet ID (2 ноды).\n\nРЕТЕСТ DETERMINISTIC-FIRST (DEC-082, 12 записей Workflow 07):\n- analysis_mode='deterministic_first', llm_enrichment=false (дефолт).\n- Баланс Claude ДО → Execute Workflow → баланс ПОСЛЕ → docs/STAGE_3_2_TEST_RESULTS.md.\n\nОЖИДАЕМО (deterministic_first, обогащение ВЫКЛ):\n- Claude Primary API Request НЕ выполняется. Claude Repair API Request НЕ выполняется.\n- Claude cost delta = $0. repair_used=false для всех 12.\n- parse_method: deterministic_pre_route = 10, deterministic_irrelevant_skip = 2.\n- technical_errors = 0.\n\nОжидаемая маршрутизация:\n- 1,2 Avito competitor → monitor_queue (deterministic_pre_route).\n- 3,4 Dzen competitor channel/post → monitor_queue.\n- 5 VK source_candidate → content_queue.\n- 7,8 Telegram source_candidate → content_queue.\n- 6 Yandex Maps reviews (competitor_related) → monitor_queue.\n- 12 Zoon reviews (competitor_related) → monitor_queue.\n- 9,10 irrelevant → skipped_log (deterministic_irrelevant_skip, $0).\n- 11 Banki форум (hot pattern, без контакта) → review_queue, entity_type=lead_signal, recommended_action=investigate, lead_signal_score=75, needs_manual_review=true (deterministic_pre_route).\n\nБудущий режим обогащения: установить analysis_mode='llm_enriched' + llm_enrichment=true → Claude вызывается по не-irrelevant записям, fallback по det сохраняется.",
        "height": 700,
        "width": 640,
        "color": 4
      }
    },
    {
      "id": "a8000000-0000-0000-0000-000000000002",
      "name": "Manual Start",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        120,
        320
      ],
      "parameters": {}
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000cf",
      "name": "Set Analyzer Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        320,
        320
      ],
      "parameters": {
        "jsCode": "// Analyzer config. deterministic_first by default, LLM enrichment OFF. No external calls by default.\n// MSK time (DEC-083): operational timestamps + run_id use explicit Moscow time +03:00.\nfunction pad(n){return String(n).padStart(2,'0');}\nfunction moscowIsoNow(){const m=new Date(Date.now()+10800000);return m.toISOString().replace('Z','+03:00');}\nfunction moscowStamp(){const m=new Date(Date.now()+10800000);return m.getUTCFullYear()+pad(m.getUTCMonth()+1)+pad(m.getUTCDate())+'_'+pad(m.getUTCHours())+pad(m.getUTCMinutes())+pad(m.getUTCSeconds());}\nconst now=moscowIsoNow();\nconst stamp=moscowStamp();\nreturn [{ json: {\n  analysis_mode:'deterministic_first',\n  llm_enrichment:false,\n  llm_enrichment_test_mode:false,\n  llm_test_batch_indexes:[1,7,11,12],\n  test_mode:true,\n  max_records:12,\n  analyze_statuses:['approved','new'],\n  production_statuses:['approved'],\n  run_id:'touchpoint_'+stamp,\n  generated_at:now,\n  note:'deterministic_first + llm_enrichment=false: obvious records route WITHOUT Claude (deterministic_pre_route / deterministic_irrelevant_skip). Only deterministic_needs_llm=true records call Claude. Future mode: analysis_mode=llm_enriched + llm_enrichment=true enriches non-irrelevant records (deterministic fallback still applies). LLM 4-record test: set llm_enrichment_test_mode=true (sends only llm_test_batch_indexes through Claude). production: set test_mode=false -> only approved analyzed and irrelevant excluded.'\n}}];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000rd",
      "name": "Read raw_market_records",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4,
      "position": [
        540,
        320
      ],
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "operation": "read",
        "documentId": {
          "__rl": true,
          "value": "PASTE_SPREADSHEET_ID_HERE",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "raw_market_records",
          "mode": "name"
        },
        "options": {}
      },
      "credentials": {
        "googleApi": {
          "id": "PASTE_CREDENTIAL_ID_HERE",
          "name": "Google Sheets - Marketing Scout Service Account"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000fl",
      "name": "Filter & Select Records",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        760,
        320
      ],
      "parameters": {
        "jsCode": "// Select records to analyze. dedup_status=unique + approval_status in allowed. test_mode includes irrelevant.\nfunction str(v){return v==null?'':String(v).trim();}\nfunction low(v){return str(v).toLowerCase();}\nconst cfg=$('Set Analyzer Config').first().json;\nconst test_mode=(cfg.test_mode===true);\nconst allowed=(test_mode?(cfg.analyze_statuses||['approved','new']):(cfg.production_statuses||['approved'])).map(s=>String(s).toLowerCase());\nconst include_irrelevant=test_mode;\nconst max_records=parseInt(cfg.max_records)||12;\nconst run_id=cfg.run_id||'';\nconst rows=$('Read raw_market_records').all().map(i=>i.json).filter(Boolean);\nconst out=[];\nfor(const r of rows){\n  if(!str(r.record_id))continue;\n  if(low(r.dedup_status)!=='unique')continue;\n  if(!allowed.includes(low(r.approval_status)))continue;\n  const rth=low(r.record_type_hint)||'unknown';\n  if(rth==='irrelevant'&&!include_irrelevant)continue;\n  if(out.length>=max_records)break;\n  out.push({ json: {\n    record_id:str(r.record_id), agent_request_id:str(r.agent_request_id), source_type:str(r.source_type),\n    platform:str(r.platform), source_url:str(r.source_url), post_url:str(r.post_url), profile_url:str(r.profile_url),\n    profile_name:str(r.profile_name), author_handle:str(r.author_handle), published_at:str(r.published_at),\n    region_hint:str(r.region_hint), service_hint:str(r.service_hint), text_context:str(r.text_context),\n    comment_text:str(r.comment_text), contact_public:str(r.contact_public), record_type_hint:rth,\n    touchpoint_type:low(r.touchpoint_type), lead_intent_hint:low(r.lead_intent_hint), urgency_hint:low(r.urgency_hint),\n    lead_temperature:low(r.lead_temperature), competitor_related:r.competitor_related, competitor_name:str(r.competitor_name),\n    interest_topic:str(r.interest_topic), probable_need:str(r.probable_need),\n    run_id:run_id, batch_index:out.length+1\n  }});\n}\n// C2 fix (DEC-086): when llm_enrichment_test_mode=true, pre-filter the SELECTED records to llm_test_batch_indexes\n// BEFORE the processing loop, so the loop receives exactly the test fixtures (e.g. [1,7,11,12] -> 4 items).\n// batch_index was assigned over the FULL selection above, so indexes stay stable (1,7,11,12). This avoids any\n// empty return inside Build Deterministic Row, which would stall Split-in-Batches loop continuation.\nconst llm_test=(cfg.llm_enrichment_test_mode===true);\nconst testIdx=Array.isArray(cfg.llm_test_batch_indexes)?cfg.llm_test_batch_indexes.map(Number):[];\nconst before=out.length;\nlet selected=out;\nif(llm_test&&testIdx.length>0){\n  selected=out.filter(it=>testIdx.indexOf(Number(it.json.batch_index))>=0);\n}\nconst after=selected.length;\nfor(const it of selected){\n  it.json.selected_count_before_test_filter=before;\n  it.json.selected_count_after_test_filter=after;\n  it.json.llm_enrichment_test_mode=llm_test;\n  it.json.llm_test_batch_indexes=testIdx;\n}\nreturn selected;"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000lp",
      "name": "Loop Over Items",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        980,
        320
      ],
      "parameters": {
        "options": {}
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000pr",
      "name": "Prepare Record",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        440
      ],
      "parameters": {
        "jsCode": "// Per-record holder. Builds analyzer text AND a DETERMINISTIC classification (det) from intake hints.\n// deterministic_first (DEC-082): det is the PRIMARY classifier. Claude is OPTIONAL enrichment.\n// det decides (a) is_irrelevant pre-Claude, (b) deterministic_needs_llm gate, (c) the row when Claude is skipped,\n// (d) the fallback if Claude+repair fail.\nfunction str(v){return v==null?'':String(v).trim();}\nfunction low(v){return str(v).toLowerCase();}\nfunction cleanContact(raw){\n  const s=String(raw||'').trim();\n  if(!s)return '';\n  if(/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}/.test(s))return s;\n  if(/t\\.me\\/|wa\\.me\\//i.test(s))return s;\n  if(/(^|[^A-Za-z0-9_])@[A-Za-z0-9_]{4,}/.test(s))return s;\n  const digits=s.replace(/\\D/g,'');\n  if(digits.length>=10&&digits.length<=11)return s;\n  return '';\n}\nfunction host(u){try{return new URL(str(u)).hostname.replace(/^www\\./,'');}catch(e){return '';}}\nfunction classifyDeterministic(r){\n  const rth=low(r.record_type_hint), tpt=low(r.touchpoint_type), lit=low(r.lead_intent_hint);\n  const urg=low(r.urgency_hint), temp=low(r.lead_temperature);\n  const compRelated=(r.competitor_related===true||low(r.competitor_related)==='true');\n  const svcRaw=low(r.service_hint);\n  const text=str(r.text_context)+' '+str(r.comment_text)+' '+str(r.interest_topic)+' '+str(r.probable_need);\n  const contact=cleanContact(r.contact_public);\n  const hasContact=contact!=='';\n  const hasInsight=(str(r.comment_text)!=='');\n  const allowedSvc=['secured_auto_loan','secured_real_estate_loan','pts_loan','refinancing','mortgage_adjacent','generic_lending','credit_broker','consumer_credit'];\n  function svcNorm(def){ if(allowedSvc.indexOf(svcRaw)>=0)return svcRaw; const t=text.toLowerCase(); if(t.indexOf('птс')>=0||t.indexOf('pts')>=0)return 'pts_loan'; if(t.indexOf('брокер')>=0)return 'credit_broker'; return def||'unknown'; }\n  function company(){ const c=str(r.competitor_name); if(c)return c; const p=str(r.profile_name); if(p)return p; const h=host(r.source_url||r.profile_url); if(h)return h; return ''; }\n  // 1. irrelevant -> skipped_log (pre-Claude, $0)\n  if(rth==='irrelevant'||tpt==='irrelevant_source'||tpt==='weak_market_noise'){\n    return { is_irrelevant:true, deterministic_needs_llm:false, entity_type:'irrelevant', route:'skipped_log', recommended_action:'ignore', company_name:'', service_type:'unknown', region:str(r.region_hint), competitor_strength:1, lead_signal_score:1, content_idea_score:1, quality_score:1, needs_manual_review:false, status:'skipped', detected_need:'', offer_text:'', contact:'' };\n  }\n  // 2. competitor activity -> monitor_queue\n  if(rth==='competitor_activity'||(compRelated&&tpt.indexOf('competitor')>=0)){\n    const clear=/₽|руб|цена|ставк|%|оффер|offer|услуг|помощь|объявлен|канал|кейс|usp|преимуществ/i.test(text);\n    return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'competitor', route:'monitor_queue', recommended_action:'monitor', company_name:company(), service_type:svcNorm('generic_lending'), region:str(r.region_hint), competitor_strength:clear?78:70, lead_signal_score:1, content_idea_score:hasInsight?30:1, quality_score:clear?80:72, needs_manual_review:false, status:'analyzed', detected_need:'', offer_text:str(r.interest_topic), contact:contact };\n  }\n  // 4. review source (handle before generic source_candidate). DEC-087: ONLY a NAMED competitor\n  // (competitor_name set) is treated as a strong competitor -> monitor_queue. A generic category/\n  // listing/review directory (e.g. a Zoon \"кредитные брокеры в Москве\" category page) is a CONTENT/\n  // monitoring source -> content_queue, content_idea, competitor_strength capped (<=45), no \"strong competitor\".\n  if(tpt==='review_source'){\n    const specificCompetitor=(str(r.competitor_name)!=='');\n    if(specificCompetitor){\n      return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'competitor', route:'monitor_queue', recommended_action:'monitor', company_name:company(), service_type:svcNorm('generic_lending'), region:str(r.region_hint), competitor_strength:70, lead_signal_score:1, content_idea_score:35, quality_score:68, needs_manual_review:false, status:'analyzed', detected_need:str(r.probable_need), offer_text:str(r.interest_topic), contact:contact };\n    }\n    return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'content_idea', route:'content_queue', recommended_action:'create_content', company_name:'', service_type:svcNorm('unknown'), region:str(r.region_hint), competitor_strength:(compRelated?45:1), lead_signal_score:1, content_idea_score:70, quality_score:68, needs_manual_review:true, status:'analyzed', detected_need:str(r.probable_need), offer_text:str(r.interest_topic), contact:contact };\n  }\n  // 3. source candidate / market signal -> content_queue\n  if(rth==='market_signal'&&tpt==='source_candidate'){\n    return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'content_idea', route:'content_queue', recommended_action:'create_content', company_name:'', service_type:svcNorm('unknown'), region:str(r.region_hint), competitor_strength:1, lead_signal_score:1, content_idea_score:55, quality_score:52, needs_manual_review:true, status:'analyzed', detected_need:'', offer_text:str(r.interest_topic), contact:contact };\n  }\n  // 5. question / objection / hot pattern -> results (with contact) or review_queue\n  if(rth==='question_objection'||temp==='hot'||lit==='high'||urg==='high'){\n    if(hasContact){\n      return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'lead_signal', route:'results', recommended_action:'contact', company_name:'', service_type:svcNorm('unknown'), region:str(r.region_hint), competitor_strength:1, lead_signal_score:85, content_idea_score:40, quality_score:80, needs_manual_review:false, status:'analyzed', detected_need:str(r.probable_need)||str(r.interest_topic), offer_text:str(r.interest_topic), contact:contact };\n    }\n    return { is_irrelevant:false, deterministic_needs_llm:false, entity_type:'lead_signal', route:'review_queue', recommended_action:'investigate', company_name:'', service_type:svcNorm('unknown'), region:str(r.region_hint), competitor_strength:1, lead_signal_score:75, content_idea_score:40, quality_score:70, needs_manual_review:true, status:'analyzed', detected_need:str(r.probable_need)||str(r.interest_topic), offer_text:str(r.interest_topic), contact:contact };\n  }\n  // 6. fallback unknown -> review_queue content_idea (classifiable but uncertain -> ask Claude when available)\n  return { is_irrelevant:false, deterministic_needs_llm:true, entity_type:'content_idea', route:'review_queue', recommended_action:'investigate', company_name:'', service_type:svcNorm('unknown'), region:str(r.region_hint), competitor_strength:1, lead_signal_score:1, content_idea_score:40, quality_score:40, needs_manual_review:true, status:'analyzed', detected_need:'', offer_text:str(r.interest_topic), contact:contact };\n}\nfunction moscowIsoNow(){const m=new Date(Date.now()+10800000);return m.toISOString().replace('Z','+03:00');}\nconst r=$json;\nconst cfg=$('Set Analyzer Config').first().json;\nconst llm_enrichment=(cfg.llm_enrichment===true);\nconst llm_test=(cfg.llm_enrichment_test_mode===true);\nconst testIdx=Array.isArray(cfg.llm_test_batch_indexes)?cfg.llm_test_batch_indexes.map(Number):[];\nconst now=moscowIsoNow();\nlet combined=str(r.text_context);\nif(str(r.comment_text))combined+=' || КОММЕНТАРИЙ: '+str(r.comment_text);\nif(str(r.interest_topic))combined+=' || ТЕМА: '+str(r.interest_topic);\nif(str(r.probable_need))combined+=' || ВЕРОЯТНАЯ ПОТРЕБНОСТЬ: '+str(r.probable_need);\nconst det=classifyDeterministic(r);\n// LLM gate (DEC-082): call Claude only for non-irrelevant records when enrichment is ON, or when det is uncertain.\n// llm_enrichment_test_mode (Part C): restrict Claude to the listed fixture batch_indexes only (e.g. [1,7,11,12]).\nlet call_claude;\nif(llm_test){\n  call_claude=(!det.is_irrelevant)&&(testIdx.length>0?(testIdx.indexOf(Number(r.batch_index))>=0):true);\n} else {\n  call_claude=(!det.is_irrelevant)&&(llm_enrichment===true||det.deterministic_needs_llm===true);\n}\nreturn [{ json: Object.assign({}, r, { parsed_at:now, analyzer_text:combined.substring(0,3500), det:det, is_irrelevant:det.is_irrelevant, deterministic_needs_llm:det.deterministic_needs_llm, call_claude:call_claude, analysis_mode:cfg.analysis_mode||'deterministic_first', llm_enrichment:llm_enrichment, llm_enrichment_test_mode:llm_test }) }];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000ii",
      "name": "IF Call Claude?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        1420,
        440
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "a8-if-callclaude-01",
              "leftValue": "={{ $json.call_claude }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000sk",
      "name": "Build Deterministic Row",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1640,
        280
      ],
      "parameters": {
        "jsCode": "// DETERMINISTIC-FIRST row builder (DEC-082). Handles BOTH:\n//  - irrelevant records -> skipped_log (parse_method=deterministic_irrelevant_skip)\n//  - obvious classifiable records routed WITHOUT Claude (parse_method=deterministic_pre_route)\n// Claude is NOT called for these rows ($0). 35 business fields on every path.\nfunction str(v){return v==null?'':String(v).trim();}\nfunction moscowIsoNow(){const m=new Date(Date.now()+10800000);return m.toISOString().replace('Z','+03:00');}\nconst src=$('Prepare Record').first().json;\nconst det=src.det||{};\n// Note (DEC-086): C2 test filtering happens in Filter & Select Records (pre-loop). This node never\n// returns [] -- non-target test records never reach it -- so loop continuation is never broken.\nconst now=moscowIsoNow();\nconst irrel=(det.is_irrelevant===true);\nconst parse_method=irrel?'deterministic_irrelevant_skip':'deterministic_pre_route';\nconst processing_status=irrel?'business_skip':'parsed_success';\nconst reason=irrel\n  ?'Запись помечена нерелевантной на этапе приёма (record_type_hint=irrelevant или touchpoint_type=irrelevant_source/weak_market_noise). Claude не вызывался — нулевая стоимость. Действие: игнорировать.'\n  :('Детерминированная классификация по подсказкам приёма raw_market_records (deterministic_first, llm_enrichment=false): запись однозначно классифицируема без Claude. Маршрут: '+(det.route||'')+'. Действие: '+(det.recommended_action||'')+'. Claude не вызывался — нулевая стоимость.');\nreturn [{ json: {\n  created_at:now, source_type:str(src.source_type), platform:str(src.platform), source_url:str(src.source_url),\n  parsed_at:str(src.parsed_at)||now, published_at:str(src.published_at), freshness_status:'unknown',\n  entity_type:det.entity_type||'irrelevant', company_name:str(det.company_name), profile_name:str(src.profile_name), profile_url:str(src.profile_url),\n  region:str(det.region)||str(src.region_hint), service_type:det.service_type||'unknown', offer_text:str(det.offer_text), terms:'', contact_public:str(det.contact),\n  text_context:str(src.analyzer_text||src.text_context), detected_need:str(det.detected_need),\n  competitor_strength:det.competitor_strength||1, lead_signal_score:det.lead_signal_score||1, content_idea_score:det.content_idea_score||1, quality_score:det.quality_score||1,\n  reason:reason,\n  recommended_action:det.recommended_action||'ignore', status:det.status||(irrel?'skipped':'analyzed'), processing_status:processing_status,\n  parse_method:parse_method, parse_error:'', raw_response_preview:'',\n  route:det.route||'skipped_log', needs_manual_review:(det.needs_manual_review===true), repair_used:false, repair_status:'',\n  run_id:str(src.run_id), batch_index:src.batch_index||0\n}}];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000bp",
      "name": "Build Primary Claude Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1640,
        600
      ],
      "parameters": {
        "jsCode": "const r=$('Prepare Record').first().json;\nconst det=r.det||{};\nfunction cap(s,n){return (s==null?'':String(s)).substring(0,n);}\n// DEC-087 C3: branch a SHORTER compact prompt for review_source (Zoon JSON reliability) and inject deterministic\n// HINTS (expected entity/action/route, no_contact_safety, forbidden_phrases) into the payload.\n// DEC-088 C4: source_candidate / social channels (Telegram) use a SPECIALIZED ULTRA-SHORT 7-key schema below —\n// a smaller surface improves strict-JSON reliability and removes the Telegram fallback.\nconst tpt=String(r.touchpoint_type||'').toLowerCase();\nconst rth=String(r.record_type_hint||'').toLowerCase();\nconst plat=String(r.platform||'').toLowerCase();\nconst stype=String(r.source_type||'').toLowerCase();\nconst hasContactDet=String(det.contact||'').trim()!=='';\nconst isSourceCandidate=(rth==='market_signal'&&tpt==='source_candidate')||(stype==='social_channel')||(plat==='telegram'&&String(det.entity_type||'')==='content_idea'&&!hasContactDet);\nconst isReview=(tpt==='review_source');\nif(isSourceCandidate){\n  // Task A — specialized compact enrichment for source_candidate / Telegram / social_channel.\n  const scSystem=`Return JSON only. No prose. No markdown. No code fences. No thinking. The first character must be { and the last character must be }.\\nUse only the given record. Do not browse. Do not fetch. Do not verify URLs.\\nThis is a public monitoring / content source, NOT a direct lead and NOT outreach. Do not mention contacting anyone. Do not call it a direct lead. Do not claim users ask questions unless text_context explicitly says so.\\nFill exactly these keys: profile_name, service_type, offer_text, detected_need, reason, content_idea_score, quality_score.\\nreason: Russian, max 2 sentences, monitoring/content framing.\\nservice_type in [secured_auto_loan,secured_real_estate_loan,pts_loan,refinancing,mortgage_adjacent,generic_lending,credit_broker,consumer_credit,unknown].\\ncontent_idea_score and quality_score are integers 1-100.\\nDo NOT output company_name, route, entity_type, recommended_action, lead_signal_score or competitor_strength.`;\n  const scSchema={profile_name:\"\",service_type:\"\",offer_text:\"\",detected_need:\"\",reason:\"\",content_idea_score:55,quality_score:55};\n  const scRecord={task:'enrich_source_candidate',platform:r.platform||'',profile_name:r.profile_name||'',profile_url:r.profile_url||'',text_context:cap(r.analyzer_text||r.text_context||'',900),interest_topic:r.interest_topic||'',service_hint:r.service_hint||'',deterministic_entity_type:'content_idea',deterministic_action:'create_content'};\n  const scUser=JSON.stringify(Object.assign({},scRecord,{output_schema:scSchema}));\n  return [{ json: { model:'claude-sonnet-4-6', max_tokens:500, temperature:0, thinking:{type:'disabled'}, system:scSystem, messages:[{ role:'user', content:scUser }] } }];\n}\nconst compact=isReview;\nconst forbidden=['спрос растёт','активно задают вопросы','много лидов','высокая конверсия'];\nif(!hasContactDet){forbidden.push('обратиться напрямую','написать','позвонить','связаться','можно использовать как источник лидов');}\nlet systemPrompt;\nif(compact){\nsystemPrompt = `You are a JSON-only enrichment formatter for business touchpoint records (secured lending, Moscow/MO: PTS / auto / real-estate collateral).\nYou cannot browse, fetch, open or verify anything. Use only ORIGINAL_RECORD, DETERMINISTIC_ROW and HINTS.\nReturn exactly one JSON object matching OUTPUT_SCHEMA. The first character must be { and the last character must be }. No markdown, no code fences, no prose, no comments, no thinking. Nothing before or after the JSON.\nDo not write \"fetching\", \"checking\", \"analyzing\", \"проверяю\", \"анализирую\".\nKeep entity_type = HINTS.expected_entity_type and recommended_action = HINTS.expected_action exactly. Do not change them.\nThis record is a MONITORING / CONTENT source, NOT a direct lead and NOT direct outreach. Frame reason as an \"источник мониторинга тем, вопросов и рыночных сигналов\".\nreason: 1-2 short Russian sentences, only facts present in ORIGINAL_RECORD.\ncompany_name, profile_name, region, service_type, offer_text, terms, detected_need: fill only if directly supported by ORIGINAL_RECORD, else \"\".\ncontact_public: only if a phone/email/@handle/t.me link is literally present in ORIGINAL_RECORD text; never invent one; else \"\".\nNever write any phrase in HINTS.forbidden_phrases. Do not claim a direct lead, a contact, demand growth, more leads, high conversion or any trend not present in ORIGINAL_RECORD.\nlead_signal_score, competitor_strength, content_idea_score, quality_score: integers 1-100, close to DETERMINISTIC_ROW. entity_type in [lead_signal,competitor,content_idea,irrelevant]. service_type in [secured_auto_loan,secured_real_estate_loan,pts_loan,refinancing,mortgage_adjacent,generic_lending,credit_broker,consumer_credit,unknown].`;\n} else {\nsystemPrompt = `You are a JSON-only enrichment formatter for business touchpoint records (secured lending, Moscow/MO: PTS / auto / real-estate collateral).\nYou cannot browse, fetch, open, or verify URLs. Use only ORIGINAL_RECORD, DETERMINISTIC_ROW and HINTS.\nReturn exactly one JSON object matching OUTPUT_SCHEMA. The first character must be { and the last character must be }.\nNo markdown. No code fences. No prose. No comments. No thinking. No text before or after the JSON.\nDo not write \"fetching\", \"checking\", \"analyzing\", \"проверяю\", \"анализирую\". Do not add external facts.\nEnrich ONLY these fields; copy DETERMINISTIC_ROW values when unsure:\n- reason: 1-2 concise Russian sentences (what + why + next action), only facts present in ORIGINAL_RECORD.\n- company_name, profile_name, region, service_type, offer_text, terms, detected_need: fill only if directly supported by ORIGINAL_RECORD, else \"\".\n- contact_public: only if a phone/email/@handle/t.me link is literally present in ORIGINAL_RECORD text; never invent one; else \"\".\nKeep recommended_action = HINTS.expected_action and entity_type = HINTS.expected_entity_type unless ORIGINAL_RECORD clearly contradicts them.\nIf HINTS.no_contact_safety is true there is NO usable public contact: NEVER instruct to reach out — do not write «обратиться напрямую», «написать», «позвонить», «связаться»; keep recommended_action = HINTS.expected_action (investigate) and say the record requires manual review.\nNever write any phrase in HINTS.forbidden_phrases. Do not claim demand growth, more leads, high conversion or any market trend that is not literally present in ORIGINAL_RECORD.\n- lead_signal_score, competitor_strength, content_idea_score, quality_score: integers 1-100, close to DETERMINISTIC_ROW.\nDo NOT return market_signal as entity_type; use content_idea for a market signal and say so in reason.\nentity_type in [lead_signal,competitor,content_idea,irrelevant]. recommended_action in [contact,investigate,monitor,create_content,add_to_semantics,ignore]. service_type in [secured_auto_loan,secured_real_estate_loan,pts_loan,refinancing,mortgage_adjacent,generic_lending,credit_broker,consumer_credit,unknown].\nFor an Avito competitor listing: enrich the competitor offer and reason. For a Banki forum hot pattern without a public contact: keep investigate/review logic, do not set a contact, follow HINTS.no_contact_safety. For Zoon/Yandex reviews: extract market pain / content insight, never call a generic review directory a strong competitor.\nIf uncertain, preserve DETERMINISTIC_ROW values.`;\n}\nconst OUTPUT_SCHEMA={company_name:\"\",profile_name:\"\",region:\"\",service_type:\"\",offer_text:\"\",terms:\"\",contact_public:\"\",detected_need:\"\",reason:\"\",recommended_action:\"\",entity_type:\"\",lead_signal_score:1,competitor_strength:1,content_idea_score:1,quality_score:1};\nconst ORIGINAL_RECORD={source_type:r.source_type||'',platform:r.platform||'',record_type_hint:r.record_type_hint||'',touchpoint_type:r.touchpoint_type||'',region_hint:r.region_hint||'',service_hint:r.service_hint||'',contact_public:r.contact_public||'',text_context:cap(r.analyzer_text||r.text_context||'',1200)};\nconst DETERMINISTIC_ROW={entity_type:(det.entity_type||''),recommended_action:(det.recommended_action||''),service_type:(det.service_type||'unknown'),company_name:(det.company_name||''),region:(det.region||r.region_hint||''),offer_text:(det.offer_text||''),detected_need:(det.detected_need||''),contact_public:(det.contact||''),lead_signal_score:(det.lead_signal_score||1),competitor_strength:(det.competitor_strength||1),content_idea_score:(det.content_idea_score||1),quality_score:(det.quality_score||1)};\nconst HINTS={expected_entity_type:(det.entity_type||''),expected_action:(det.recommended_action||''),expected_route:(det.route||''),no_contact_safety:!hasContactDet,forbidden_phrases:forbidden};\nconst userMsg=JSON.stringify({ORIGINAL_RECORD:ORIGINAL_RECORD,DETERMINISTIC_ROW:DETERMINISTIC_ROW,HINTS:HINTS,OUTPUT_SCHEMA:OUTPUT_SCHEMA});\nreturn [{ json: { model:'claude-sonnet-4-6', max_tokens:700, temperature:0, thinking:{type:'disabled'}, system:systemPrompt, messages:[{ role:'user', content:userMsg }] } }];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000c1",
      "name": "Claude Primary API Request",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1860,
        600
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://aiprimetech.io/v1/messages",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "anthropic-version",
              "value": "2023-06-01"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ JSON.stringify({ model: $json.model, max_tokens: $json.max_tokens, temperature: $json.temperature, thinking: $json.thinking, system: $json.system, messages: $json.messages }) }}",
        "options": {}
      },
      "credentials": {
        "httpHeaderAuth": {
          "id": "PASTE_CREDENTIAL_ID_HERE",
          "name": "Claude API - Marketing Scout"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000pp",
      "name": "Parse Primary JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2080,
        600
      ],
      "parameters": {
        "jsCode": "const response=$json;\nfunction cap(s,n){return (s==null?'':String(s)).substring(0,n);}\nfunction extractBalanced(s){\n  const start=s.indexOf('{'); if(start===-1)return null;\n  let depth=0,inStr=false,esc=false;\n  for(let i=start;i<s.length;i++){const c=s[i];\n    if(inStr){ if(esc){esc=false;} else if(c==='\\\\'){esc=true;} else if(c==='\"'){inStr=false;} continue; }\n    if(c==='\"'){inStr=true;continue;}\n    if(c==='{')depth++; else if(c==='}'){depth--; if(depth===0)return s.substring(start,i+1);}\n  }\n  return null;\n}\nfunction joinText(resp){\n  if(!resp||!Array.isArray(resp.content))return {ok:false,text:'',types:'no_content_array'};\n  const types=resp.content.map(c=>(c&&c.type)||'unknown').join(',');\n  const parts=resp.content.filter(c=>c&&c.type==='text'&&typeof c.text==='string').map(c=>c.text);\n  return {ok:parts.length>0,text:parts.join('\\n').trim(),types:types};\n}\nfunction tryParse(text){\n  let txt=String(text).trim();\n  txt=txt.replace(/^```json\\s*/i,'').replace(/```\\s*$/,'').trim();\n  txt=txt.replace(/^```\\s*/,'').replace(/```\\s*$/,'').trim();\n  const norm=(x)=>x.replace(/[‘’]/g,\"'\").replace(/[“”«»]/g,'\"');\n  try{return {ok:true,obj:JSON.parse(norm(txt))};}catch(e){}\n  const bal=extractBalanced(txt);\n  if(bal){try{return {ok:true,obj:JSON.parse(norm(bal))};}catch(e){}}\n  const s=txt.indexOf('{'),e2=txt.lastIndexOf('}');\n  if(s!==-1&&e2!==-1&&e2>s){const slice=txt.substring(s,e2+1);try{return {ok:true,obj:JSON.parse(norm(slice))};}catch(e){}}\n  return {ok:false};\n}\nfunction fail(err,prev){\n  return [{ json:{ parse_ok:false, parse_method:'primary_json', enrichment:null, parse_error:cap(err,300), raw_response_preview:cap(prev,500) } }];\n}\nif(response.error||response.status>=400){\n  return fail('Primary HTTP error: '+cap(JSON.stringify(response.error||response.status),300),'non_json_non_text_or_thinking_response');\n}\nconst jt=joinText(response);\nif(!jt.text){\n  // thinking/signature/tool only -> no usable text\n  return fail('Primary: no text item ('+jt.types+')','non_json_non_text_or_thinking_response');\n}\nconst rawPreview=cap(jt.text,500);\nconst res=tryParse(jt.text);\nif(!res.ok){\n  return fail('Primary: text present but JSON invalid',rawPreview);\n}\nreturn [{ json:{ parse_ok:true, parse_method:'primary_json', enrichment:res.obj, parse_error:'', raw_response_preview:rawPreview } }];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000ip",
      "name": "IF Primary Parse OK?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        2300,
        600
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "a8-if-parse-01",
              "leftValue": "={{ $json.parse_ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000rq",
      "name": "Build Repair Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2300,
        820
      ],
      "parameters": {
        "jsCode": "const input=$json;\nconst r=$('Prepare Record').first().json;\nconst det=r.det||{};\nfunction cap(s,n){return (s==null?'':String(s)).substring(0,n);}\nconst rawPrev=cap(input.raw_response_preview||'empty',400);\n// DEC-088 C4 (Task C): source_candidate / social channels repair into the SAME ultra-short 7-key schema —\n// never inflate to the full 15-key general schema. RAW_RESPONSE is the capped sanitized preview only.\nconst tpt=String(r.touchpoint_type||'').toLowerCase();\nconst rth=String(r.record_type_hint||'').toLowerCase();\nconst plat=String(r.platform||'').toLowerCase();\nconst stype=String(r.source_type||'').toLowerCase();\nconst hasContactSc=String(det.contact||'').trim()!=='';\nconst isSourceCandidate=(rth==='market_signal'&&tpt==='source_candidate')||(stype==='social_channel')||(plat==='telegram'&&String(det.entity_type||'')==='content_idea'&&!hasContactSc);\nif(isSourceCandidate){\n  const scSystem=`You repair JSON only. Return exactly one JSON object with ONLY these keys: profile_name, service_type, offer_text, detected_need, reason, content_idea_score, quality_score. First char {, last char }. No prose. No markdown. No code fences. No thinking. Do not say \"Repairing\".\\nIf RAW_RESPONSE contains JSON, normalize it to these 7 keys; otherwise build them from RECORD. This is a public monitoring/content source — reason in Russian, max 2 sentences, monitoring framing, NOT a direct lead, no contact, no outreach. service_type in [secured_auto_loan,secured_real_estate_loan,pts_loan,refinancing,mortgage_adjacent,generic_lending,credit_broker,consumer_credit,unknown]. content_idea_score and quality_score are integers 1-100. Do NOT output company_name, route, entity_type, recommended_action, lead_signal_score or competitor_strength.`;\n  const scSchema={profile_name:\"\",service_type:\"\",offer_text:\"\",detected_need:\"\",reason:\"\",content_idea_score:55,quality_score:55};\n  const scRecord={platform:r.platform||'',profile_name:r.profile_name||'',text_context:cap(r.analyzer_text||r.text_context||'',500),interest_topic:r.interest_topic||'',service_hint:r.service_hint||''};\n  const scUser=JSON.stringify({RAW_RESPONSE_SANITIZED:rawPrev,RECORD:scRecord,OUTPUT_SCHEMA:scSchema});\n  return [{ json:{ model:'claude-sonnet-4-6', max_tokens:400, temperature:0, thinking:{type:'disabled'}, system:scSystem, messages:[{ role:'user', content:scUser }] } }];\n}\nconst repairSystem=`You repair JSON only. Return exactly one JSON object matching OUTPUT_SCHEMA. First char {, last char }. No prose. No markdown. No code fences. No comments. No thinking. Do not say \"Repairing\". Do not write \"fetching\"/\"analyzing\".\nIf RAW_RESPONSE contains valid JSON, extract and normalize it to OUTPUT_SCHEMA. If RAW_RESPONSE contains no valid JSON, build a safe JSON object from ORIGINAL_RECORD and DETERMINISTIC_ROW (prefer DETERMINISTIC_ROW values). Never invent a contact. Do not return market_signal as entity_type; use content_idea.\nKeep entity_type = HINTS.expected_entity_type and recommended_action = HINTS.expected_action. Never write any phrase in HINTS.forbidden_phrases. If HINTS.no_contact_safety is true, never instruct to reach out (no «обратиться напрямую»/«написать»/«позвонить»/«связаться») and say the record requires manual review. Do not claim demand growth, more leads or high conversion unless present in ORIGINAL_RECORD. Scores are integers 1-100. reason is 1-2 short Russian sentences.`;\nconst OUTPUT_SCHEMA={company_name:\"\",profile_name:\"\",region:\"\",service_type:\"\",offer_text:\"\",terms:\"\",contact_public:\"\",detected_need:\"\",reason:\"\",recommended_action:\"\",entity_type:\"\",lead_signal_score:1,competitor_strength:1,content_idea_score:1,quality_score:1};\nconst ORIGINAL_RECORD={source_type:r.source_type||'',platform:r.platform||'',record_type_hint:r.record_type_hint||'',touchpoint_type:r.touchpoint_type||'',region_hint:r.region_hint||'',service_hint:r.service_hint||'',contact_public:r.contact_public||'',text_context:cap(r.analyzer_text||r.text_context||'',500)};\nconst DETERMINISTIC_ROW={entity_type:(det.entity_type||''),recommended_action:(det.recommended_action||''),service_type:(det.service_type||'unknown'),company_name:(det.company_name||''),region:(det.region||r.region_hint||''),offer_text:(det.offer_text||''),detected_need:(det.detected_need||''),contact_public:(det.contact||''),lead_signal_score:(det.lead_signal_score||1),competitor_strength:(det.competitor_strength||1),content_idea_score:(det.content_idea_score||1),quality_score:(det.quality_score||1)};\nconst hasContactDet=String(det.contact||'').trim()!=='';\nconst forbidden=['спрос растёт','активно задают вопросы','много лидов','высокая конверсия'];\nif(!hasContactDet){forbidden.push('обратиться напрямую','написать','позвонить','связаться','можно использовать как источник лидов');}\nconst HINTS={expected_entity_type:(det.entity_type||''),expected_action:(det.recommended_action||''),expected_route:(det.route||''),no_contact_safety:!hasContactDet,forbidden_phrases:forbidden};\nconst userMsg=JSON.stringify({RAW_RESPONSE_SANITIZED:rawPrev,ORIGINAL_RECORD:ORIGINAL_RECORD,DETERMINISTIC_ROW:DETERMINISTIC_ROW,HINTS:HINTS,OUTPUT_SCHEMA:OUTPUT_SCHEMA});\nreturn [{ json:{ model:'claude-sonnet-4-6', max_tokens:600, temperature:0, thinking:{type:'disabled'}, system:repairSystem, messages:[{ role:'user', content:userMsg }] } }];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000c2",
      "name": "Claude Repair API Request",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2520,
        820
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://aiprimetech.io/v1/messages",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "anthropic-version",
              "value": "2023-06-01"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ JSON.stringify({ model: $json.model, max_tokens: $json.max_tokens, temperature: $json.temperature, thinking: $json.thinking, system: $json.system, messages: $json.messages }) }}",
        "options": {}
      },
      "credentials": {
        "httpHeaderAuth": {
          "id": "PASTE_CREDENTIAL_ID_HERE",
          "name": "Claude API - Marketing Scout"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000pr2",
      "name": "Parse Repaired JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2740,
        820
      ],
      "parameters": {
        "jsCode": "const response=$json;\nconst primary=$('Parse Primary JSON').first().json;\nfunction cap(s,n){return (s==null?'':String(s)).substring(0,n);}\nfunction extractBalanced(s){\n  const start=s.indexOf('{'); if(start===-1)return null;\n  let depth=0,inStr=false,esc=false;\n  for(let i=start;i<s.length;i++){const c=s[i];\n    if(inStr){ if(esc){esc=false;} else if(c==='\\\\'){esc=true;} else if(c==='\"'){inStr=false;} continue; }\n    if(c==='\"'){inStr=true;continue;}\n    if(c==='{')depth++; else if(c==='}'){depth--; if(depth===0)return s.substring(start,i+1);}\n  }\n  return null;\n}\nfunction joinText(resp){\n  if(!resp||!Array.isArray(resp.content))return {ok:false,text:'',types:'no_content_array'};\n  const types=resp.content.map(c=>(c&&c.type)||'unknown').join(',');\n  const parts=resp.content.filter(c=>c&&c.type==='text'&&typeof c.text==='string').map(c=>c.text);\n  return {ok:parts.length>0,text:parts.join('\\n').trim(),types:types};\n}\nfunction tryParse(text){\n  let txt=String(text).trim();\n  txt=txt.replace(/^```json\\s*/i,'').replace(/```\\s*$/,'').trim();\n  txt=txt.replace(/^```\\s*/,'').replace(/```\\s*$/,'').trim();\n  const norm=(x)=>x.replace(/[‘’]/g,\"'\").replace(/[“”«»]/g,'\"');\n  try{return {ok:true,obj:JSON.parse(norm(txt))};}catch(e){}\n  const bal=extractBalanced(txt);\n  if(bal){try{return {ok:true,obj:JSON.parse(norm(bal))};}catch(e){}}\n  const s=txt.indexOf('{'),e2=txt.lastIndexOf('}');\n  if(s!==-1&&e2!==-1&&e2>s){const slice=txt.substring(s,e2+1);try{return {ok:true,obj:JSON.parse(norm(slice))};}catch(e){}}\n  return {ok:false};\n}\nconst primaryErr=cap(primary.parse_error||'unknown',300);\nconst primaryPrev=cap(primary.raw_response_preview||'',500);\nfunction fallback(repairErr,prev){\n  // deterministic fallback after primary+repair failed (DEC-081/085)\n  return [{ json:{ parse_ok:false, parse_method:'deterministic_fallback_after_llm_fail', enrichment:null,\n    parse_error:cap('Primary: '+primaryErr+' | Repair: '+repairErr,800),\n    raw_response_preview:cap((prev&&prev.indexOf('{')>-1)?prev:'non_json_non_text_or_thinking_response',500) } }];\n}\nif(response.error||response.status>=400){ return fallback('Repair HTTP error: '+cap(JSON.stringify(response.error||response.status),200),primaryPrev); }\nconst jt=joinText(response);\nif(!jt.text){ return fallback('Repair: no text item ('+jt.types+')',primaryPrev); }\nconst res=tryParse(jt.text);\nif(!res.ok){ return fallback('Repair: JSON invalid',cap(jt.text,500)); }\nreturn [{ json:{ parse_ok:true, parse_method:'repaired_json', enrichment:res.obj, parse_error:'', raw_response_preview:cap(jt.text,500) } }];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000nr",
      "name": "Merge LLM Enrichment With Deterministic Row",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2520,
        600
      ],
      "parameters": {
        "jsCode": "// MERGE LLM ENRICHMENT INTO DETERMINISTIC ROW (DEC-085).\n// Deterministic classification (det) is the source of truth for route/recommended_action/entity/contact safety.\n// Claude enrichment only improves descriptive fields + scores (within deterministic floors).\n// parse_method: primary_json | repaired_json | deterministic_fallback_after_llm_fail | technical_error.\nconst data=$json;\nconst src=$('Prepare Record').first().json;\nconst det=src.det||{};\nfunction str(v){return v==null?'':String(v).trim();}\nfunction clamp(v){const n=parseInt(v);return isNaN(n)?1:Math.min(100,Math.max(1,n));}\nfunction truncate(s,n){return (s==null?'':String(s)).substring(0,n);}\nfunction cleanContact(raw){\n  const s=String(raw||'').trim();\n  if(!s)return '';\n  if(/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}/.test(s))return s;\n  if(/t\\.me\\/|wa\\.me\\//i.test(s))return s;\n  if(/(^|[^A-Za-z0-9_])@[A-Za-z0-9_]{4,}/.test(s))return s;\n  const digits=s.replace(/\\D/g,'');\n  if(digits.length>=10&&digits.length<=11)return s;\n  return '';\n}\nfunction moscowIsoNow(){const m=new Date(Date.now()+10800000);return m.toISOString().replace('Z','+03:00');}\nconst now=moscowIsoNow();\nconst run_id=src.run_id||'';\nconst batch_index=src.batch_index||0;\nconst validRoutes=['results','review_queue','monitor_queue','content_queue','skipped_log'];\nconst allowedSvc=['secured_auto_loan','secured_real_estate_loan','pts_loan','refinancing','mortgage_adjacent','generic_lending','credit_broker','consumer_credit','unknown'];\n\n// parse_method + enrichment object\nconst enr=(data.parse_ok===true && data.enrichment && typeof data.enrichment==='object')?data.enrichment:null;\nlet parse_method = enr ? ((data.parse_method==='repaired_json')?'repaired_json':'primary_json') : 'deterministic_fallback_after_llm_fail';\n\n// route is deterministic. If det has no valid route -> technical_errors (should not happen for the fixtures).\nlet route=det.route||'';\nif(validRoutes.indexOf(route)===-1){\n  return [{ json:{\n    created_at:now, source_type:str(src.source_type), platform:str(src.platform), source_url:str(src.source_url),\n    parsed_at:str(src.parsed_at)||now, published_at:str(src.published_at), freshness_status:'unknown',\n    entity_type:'irrelevant', company_name:'', profile_name:str(src.profile_name), profile_url:str(src.profile_url),\n    region:str(src.region_hint), service_type:'unknown', offer_text:'', terms:'', contact_public:'',\n    text_context:truncate(src.analyzer_text||src.text_context||'',3500), detected_need:'',\n    competitor_strength:1, lead_signal_score:1, content_idea_score:1, quality_score:1,\n    reason:'Детерминированная классификация не дала валидного маршрута; запись отправлена в technical_errors для ручной проверки.',\n    recommended_action:'ignore', status:'analyzed', processing_status:'technical_error',\n    parse_method:'technical_error', parse_error:truncate(data.parse_error||'no_valid_det_route',800),\n    raw_response_preview:truncate(data.raw_response_preview||'non_json_non_text_or_thinking_response',500),\n    route:'technical_errors', needs_manual_review:true, repair_used:true, repair_status:'failed', run_id:run_id, batch_index:batch_index\n  }}];\n}\n\nlet entity=det.entity_type||'content_idea';\nlet action=det.recommended_action||'investigate';\nconst contact=cleanContact(det.contact||'');\nconst hasContact=contact!=='';\n\n// deterministic safety floor (belt and suspenders): no results/contact without usable contact.\nif(route==='results' && !hasContact){ route='review_queue'; action='investigate'; }\nif(route==='results' && action!=='contact'){ action='contact'; }\n\n// base (deterministic) descriptive + scores\nlet company_name=str(det.company_name);\nlet profile_name=str(src.profile_name);\nlet region=str(det.region)||str(src.region_hint);\nlet service_type=(allowedSvc.indexOf(String(det.service_type||'').toLowerCase())>=0)?String(det.service_type).toLowerCase():'unknown';\nlet offer_text=truncate(det.offer_text||'',300);\nlet terms='';\nlet detected_need=truncate(det.detected_need||'',300);\nlet reason='';\nlet leadScore=clamp(det.lead_signal_score||1);\nlet compScore=clamp(det.competitor_strength||1);\nlet contentScore=clamp(det.content_idea_score||1);\nlet qualScore=clamp(det.quality_score||1);\n\nif(enr){\n  if(str(enr.company_name)) company_name=truncate(enr.company_name,160);\n  if(str(enr.profile_name)) profile_name=truncate(enr.profile_name,160);\n  if(str(enr.region)) region=truncate(enr.region,120);\n  const es=String(enr.service_type||'').toLowerCase();\n  if(es && allowedSvc.indexOf(es)>=0) service_type=es;\n  if(str(enr.offer_text)) offer_text=truncate(enr.offer_text,300);\n  if(str(enr.terms)) terms=truncate(enr.terms,220);\n  if(str(enr.detected_need)) detected_need=truncate(enr.detected_need,300);\n  if(str(enr.reason)) reason=truncate(enr.reason,600);\n  // scores: scale a 1-10 response to 1-100, then floor at the deterministic value.\n  const rs=[enr.lead_signal_score,enr.competitor_strength,enr.content_idea_score,enr.quality_score].map(function(v){var n=parseInt(v);return isNaN(n)?0:n;});\n  const maxRaw=Math.max.apply(null,rs);\n  const scale=(maxRaw>0&&maxRaw<=10)?10:1;\n  if(rs[0]>0) leadScore=Math.max(leadScore,clamp(rs[0]*scale));\n  if(rs[1]>0) compScore=Math.max(compScore,clamp(rs[1]*scale));\n  if(rs[2]>0) contentScore=Math.max(contentScore,clamp(rs[2]*scale));\n  if(rs[3]>0) qualScore=Math.max(qualScore,clamp(rs[3]*scale));\n}\nif(!reason){\n  reason='Детерминированная классификация по подсказкам приёма raw_market_records: маршрут '+route+', действие '+action+'.'+(enr?'':' Claude не вернул валидный JSON — использована детерминированная строка (fallback).');\n}\n// Reason safety sanitizer (DEC-087). Deterministic guarantee regardless of what Claude returned:\n// (a) NO direct-outreach phrasing when there is no usable public contact;\n// (b) NO unsupported demand/volume/conversion trend claims that are not present in ORIGINAL_RECORD.\nconst origLow=String(src.analyzer_text||src.text_context||'').toLowerCase();\nif(!hasContact && /(обрат[аи]\\w*\\s+напряму|напряму\\w*|напиш\\w*|написа\\w*|позвон\\w*|связат\\w*|свяжит\\w*)/i.test(reason)){\n  reason='Рыночный лид-паттерн. Публичного контакта нет, поэтому запись требует ручной проверки ветки/автора; может использоваться для сценариев консультации и контента.';\n}\nconst trendRe=/(спрос[а-яё]*\\s+раст[а-яё]*|активно\\s+задаю[а-яё]*|много\\s+лидов|высок[а-яё]*\\s+конверс[а-яё]*)/i;\nif(trendRe.test(reason) && !trendRe.test(origLow)){\n  reason=reason.replace(new RegExp(trendRe.source,'gi'),'есть рыночный паттерн').replace(/\\s{2,}/g,' ').trim();\n}\n\n// Source-candidate / social-channel safety assertion (DEC-088 Task D): a public social source can never become\n// a lead/competitor/outreach row, regardless of LLM output. Mirrors the specialized enrichment detection.\nconst sa_tpt=String(src.touchpoint_type||'').toLowerCase();\nconst sa_rth=String(src.record_type_hint||'').toLowerCase();\nconst sa_plat=String(src.platform||'').toLowerCase();\nconst sa_stype=String(src.source_type||'').toLowerCase();\nconst isSocialSource=(sa_rth==='market_signal'&&sa_tpt==='source_candidate')||(sa_stype==='social_channel')||(sa_plat==='telegram'&&String(det.entity_type||'')==='content_idea'&&!hasContact);\nif(isSocialSource){\n  if(det.route!=='review_queue'){ route='content_queue'; }\n  entity='content_idea';\n  if(action!=='create_content'&&action!=='investigate'){ action='create_content'; }\n  const directRequest=(sa_rth==='question_objection')||/(^|[^а-яё])(нужен|нужна|нужно|ищу|подскажите|помогите|посоветуйте|кто да[ёе]т|одобрят ли|где взять)([^а-яё]|$)/i.test(origLow);\n  if(!directRequest){ leadScore=1; }\n  if(sa_rth!=='competitor_activity'){ compScore=1; }\n}\nlet rawPrev=truncate(data.raw_response_preview||'',500);\nif(parse_method==='deterministic_fallback_after_llm_fail' && (!rawPrev || rawPrev.indexOf('{')===-1)) rawPrev='non_json_non_text_or_thinking_response';\nif(parse_method==='primary_json') rawPrev=truncate(rawPrev,200);\n\nconst detFb=(parse_method==='deterministic_fallback_after_llm_fail');\nconst needs_manual_review=detFb?true:(det.needs_manual_review===true);\nconst repair_used=(parse_method==='repaired_json')||detFb;\nconst repair_status=(parse_method==='repaired_json')?'success':(detFb?'failed_fallback':'');\n\nreturn [{ json:{\n  created_at: str(src.parsed_at)||now,\n  source_type: str(src.source_type),\n  platform: str(src.platform),\n  source_url: str(src.source_url),\n  parsed_at: str(src.parsed_at)||now,\n  published_at: str(src.published_at),\n  freshness_status: 'unknown',\n  entity_type: entity,\n  company_name: company_name,\n  profile_name: profile_name,\n  profile_url: str(src.profile_url),\n  region: region,\n  service_type: service_type,\n  offer_text: offer_text,\n  terms: terms,\n  contact_public: contact,\n  text_context: truncate(src.analyzer_text||src.text_context||'',3500),\n  detected_need: detected_need,\n  competitor_strength: compScore,\n  lead_signal_score: leadScore,\n  content_idea_score: contentScore,\n  quality_score: qualScore,\n  reason: reason,\n  recommended_action: action,\n  status: det.status||'analyzed',\n  processing_status: 'parsed_success',\n  parse_method: parse_method,\n  parse_error: truncate(data.parse_error||'',800),\n  raw_response_preview: rawPrev,\n  route: route,\n  needs_manual_review: needs_manual_review,\n  repair_used: repair_used,\n  repair_status: repair_status,\n  run_id: run_id,\n  batch_index: batch_index\n}}];"
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000ap",
      "name": "Append to Dynamic Route Sheet",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4,
      "position": [
        2980,
        440
      ],
      "parameters": {
        "operation": "append",
        "documentId": {
          "__rl": true,
          "value": "PASTE_SPREADSHEET_ID_HERE",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "={{ $json.route }}",
          "mode": "name"
        },
        "columns": {
          "mappingMode": "autoMapInputData",
          "value": {},
          "matchingColumns": [],
          "schema": []
        },
        "options": {}
      },
      "credentials": {
        "googleApi": {
          "id": "PASTE_CREDENTIAL_ID_HERE",
          "name": "Google Sheets - Marketing Scout Service Account"
        }
      }
    },
    {
      "id": "a8000000-0000-0000-0000-0000000000fs",
      "name": "Final Summary Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        180
      ],
      "parameters": {
        "jsCode": "function str(v){return v==null?'':String(v).trim();}\nlet analyzed=[];let deterministic=[];\ntry{analyzed=$('Merge LLM Enrichment With Deterministic Row').all().map(i=>i.json);}catch(e){analyzed=[];}\ntry{deterministic=$('Build Deterministic Row').all().map(i=>i.json);}catch(e){deterministic=[];}\nconst all=analyzed.concat(deterministic);\nconst cfg=$('Set Analyzer Config').first().json;\nlet selected_count=0;\ntry{selected_count=$('Filter & Select Records').all().length;}catch(e){selected_count=0;}\nconst route_counts={};const entity_counts={};const parse_method_counts={};let repair_used=0;let tech=0;let detfb=0;let preroute=0;let irrelskip=0;\nfor(const r of all){\n  const rt=str(r.route)||'unknown';route_counts[rt]=(route_counts[rt]||0)+1;\n  const en=str(r.entity_type)||'unknown';entity_counts[en]=(entity_counts[en]||0)+1;\n  const pm=str(r.parse_method)||'unknown';parse_method_counts[pm]=(parse_method_counts[pm]||0)+1;\n  if(r.repair_used===true)repair_used++;\n  if(rt==='technical_errors')tech++;\n  if(pm==='deterministic_fallback_after_llm_fail')detfb++;\n  if(pm==='deterministic_pre_route')preroute++;\n  if(pm==='deterministic_irrelevant_skip')irrelskip++;\n}\nreturn [{ json: {\n  workflow:'08 - Touchpoint Analyzer',\n  run_id:cfg.run_id||'',\n  analysis_mode:cfg.analysis_mode||'deterministic_first',\n  llm_enrichment:cfg.llm_enrichment===true,\n  test_mode:cfg.test_mode===true,\n  llm_enrichment_test_mode:cfg.llm_enrichment_test_mode===true,\n  llm_test_batch_indexes:cfg.llm_test_batch_indexes||[],\n  selected_count:selected_count,\n  total_processed:all.length,\n  claude_path_rows:analyzed.length,\n  deterministic_rows:deterministic.length,\n  deterministic_pre_route:preroute,\n  deterministic_irrelevant_skip:irrelskip,\n  deterministic_fallback_after_llm_fail:detfb,\n  route_counts:route_counts,\n  entity_counts:entity_counts,\n  parse_method_counts:parse_method_counts,\n  repair_used_count:repair_used,\n  technical_errors_count:tech,\n  technical_error_count:tech,\n  next_action:'Review the 6 business tabs and record outcomes in docs/STAGE_3_2_TEST_RESULTS.md. deterministic_first + llm_enrichment=false => Claude calls=0, repair_used=0, technical_errors=0 for the 12-record fixture.'\n}}];"
      }
    }
  ],
  "connections": {
    "Manual Start": {
      "main": [
        [
          {
            "node": "Set Analyzer Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Set Analyzer Config": {
      "main": [
        [
          {
            "node": "Read raw_market_records",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read raw_market_records": {
      "main": [
        [
          {
            "node": "Filter & Select Records",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filter & Select Records": {
      "main": [
        [
          {
            "node": "Loop Over Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Loop Over Items": {
      "main": [
        [
          {
            "node": "Final Summary Output",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Prepare Record",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Record": {
      "main": [
        [
          {
            "node": "IF Call Claude?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF Call Claude?": {
      "main": [
        [
          {
            "node": "Build Primary Claude Request",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Build Deterministic Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Deterministic Row": {
      "main": [
        [
          {
            "node": "Append to Dynamic Route Sheet",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Primary Claude Request": {
      "main": [
        [
          {
            "node": "Claude Primary API Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claude Primary API Request": {
      "main": [
        [
          {
            "node": "Parse Primary JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Primary JSON": {
      "main": [
        [
          {
            "node": "IF Primary Parse OK?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF Primary Parse OK?": {
      "main": [
        [
          {
            "node": "Merge LLM Enrichment With Deterministic Row",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Build Repair Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Repair Request": {
      "main": [
        [
          {
            "node": "Claude Repair API Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claude Repair API Request": {
      "main": [
        [
          {
            "node": "Parse Repaired JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Repaired JSON": {
      "main": [
        [
          {
            "node": "Merge LLM Enrichment With Deterministic Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Append to Dynamic Route Sheet": {
      "main": [
        [
          {
            "node": "Loop Over Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Merge LLM Enrichment With Deterministic Row": {
      "main": [
        [
          {
            "node": "Append to Dynamic Route Sheet",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "a8000000-touchpoint-analyzer-v007-c4-source-candidate-schema-20260609",
  "meta": {
    "instanceId": "marketing-scout-v8-touchpoint-analyzer"
  }
}